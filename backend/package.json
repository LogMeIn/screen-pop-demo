{
  "name": "backend",
  "version": "1.0.0",
  "description": "Local backend for screen pop demo frontend",
  "main": "server.js",
  "scripts": {
    "start": "rm -rf build/ && tsc && node build/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "GoTo",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.14",
    "@types/express": "^4.17.17",
    "@types/node": "^20.6.3",
    "typescript": "^5.2.2"
  }
}
